{
  "name": "sj-capture",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Nightly share-image capture for Suffering Jukebox (runs as a Render cron job).",
  "scripts": {
    "start": "node capture.mjs",
    "list": "node capture.mjs --list",
    "dry": "node capture.mjs --dry-run --limit 1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1111.0",
    "playwright": "1.62.1"
  }
}
